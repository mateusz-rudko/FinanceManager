<?xml version="1.0" encoding="utf-8" ?>
<xct:Popup xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             x:Class="Inzynierka.Popups.DeletePopup"
             Size="300,150">
    <StackLayout Style="{StaticResource popupStackLayout}" >
        <Label Text="Do you want delete the record?" FontSize="18" HorizontalTextAlignment="Center" Margin="0,20"/>
        <Grid ColumnDefinitions="1*,1*" RowDefinitions="1*" Margin="0,20">
            <Button Style="{StaticResource popupButton}"  Text="Yes" Grid.Column="0" Grid.Row="0" Clicked="YesButtonClicked" VerticalOptions="End"/>
            <Button Style="{StaticResource popupButton}" Text="No" Grid.Column="1" Grid.Row="0" Clicked="NoButtonClicked" VerticalOptions="End"/>
        </Grid>

    </StackLayout>

</xct:Popup>